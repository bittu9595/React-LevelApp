import { AppBar, Toolbar } from "@material-ui/core"

const Footer = (props)=> {
    return (
        <div>
        <AppBar position="static">
   <Toolbar>
  Footer
   </Toolbar>
 </AppBar>
   </div>
    )
}
export default Footer;